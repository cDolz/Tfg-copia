import { Component, Input } from '@angular/core';

@Component({
  selector: 'app-event-date-selection',
  templateUrl: './event-date-selection.component.html',
  styleUrl: './event-date-selection.component.scss'
})
export class EventDateSelectionComponent {
  constructor() { 
    this.initForm();
  }

  private initForm(){
        
  }
}
